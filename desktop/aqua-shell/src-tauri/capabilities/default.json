{
  "identifier": "default",
  "description": "Default capabilities for aqua-shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-open"
  ]
}

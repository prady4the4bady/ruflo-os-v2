httpx>=0.27.0
pydantic>=2.0.0
pytest>=7.0
pytest-asyncio>=0.23
pytest-mock>=3.12
pytest-timeout>=2.2
# Optional viral posting (NOT installed by default — see note in
# prav/viral_publisher.py). Auto-posting to Reddit/Twitter/HN/Dev.to is
# disabled at the system level; these stay commented unless explicitly
# enabled by an operator who has read the platform ToS.
# praw>=7.0
# tweepy>=4.0
# PyGithub>=2.0
